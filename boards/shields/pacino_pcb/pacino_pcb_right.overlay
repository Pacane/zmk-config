#include "pacino_pcb.dtsi"

/*
 * Right half: parts on the board's FRONT face, controller on the BACK -- see the left overlay; the
 * same face swap applies, so these are the pacino README's *left*-half pins.  Bridge JP1 / JP2 to
 * the pad marked L (the mark on the face the nano sits on).
 */

&default_transform {
    col-offset = <5>;
};

&kscan0 {
    row-gpios
        = <&pro_micro 21 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* row 0: top    */
        , <&pro_micro 20 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* row 1: home   */
        , <&pro_micro 19 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* row 2: bottom */
        , <&pro_micro 18 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* row 3: extras */
        , <&pro_micro 10 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* row 4: thumbs */
        ;
    col-gpios
        = <&pro_micro 2 GPIO_ACTIVE_HIGH>    /* col 5: inner  */
        , <&pro_micro 3 GPIO_ACTIVE_HIGH>    /* col 6: index  */
        , <&pro_micro 4 GPIO_ACTIVE_HIGH>    /* col 7: middle */
        , <&pro_micro 5 GPIO_ACTIVE_HIGH>    /* col 8: ring   */
        , <&pro_micro 9 GPIO_ACTIVE_HIGH>    /* col 9: pinky  */
        ;
};
