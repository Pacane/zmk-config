# TEMPORARY: only the right half of the slim PCB build exists for now, so it plays the central (the
# role the shield's Kconfig.defconfig gives the left half). Delete this file once the left half is
# built, then: settings_reset the right half, flash the normal pacino_pcb_right and pacino_pcb_left,
# and pair the host again -- with the left half.
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y
CONFIG_ZMK_KEYBOARD_NAME="Pacino PCB"
